name: blank

on:
  push:

jobs:
  metric:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        tidy: ["convenient", "applicable", "evident", "consist", "confront"]
        breakdown: ["ad", "prisoner", "crowd", "participate"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar yu
